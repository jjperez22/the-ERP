# ================================
# Construction ERP - Render.com Environment Variables
# Copy these variables to your Render dashboard
# ================================

# ESSENTIAL VARIABLES (Required for deployment)
NODE_ENV=production
PORT=10000
DATABASE_URL=postgresql://user:password@host:port/database_name
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters-long

# AI SERVICES (Required for AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here

# REDIS (Optional - for caching and real-time features)
REDIS_URL=redis://user:password@host:port

# EMAIL CONFIGURATION (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# APPLICATION SETTINGS (Optional - with defaults)
LOG_LEVEL=info
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
MAX_FILE_SIZE=10485760

# FEATURE FLAGS (Optional - defaults to true)
ENABLE_AI_FEATURES=true
ENABLE_REAL_TIME=true
ENABLE_AUTOMATION=true
ENABLE_ANALYTICS=true

# UPLOAD CONFIGURATION (Optional)
UPLOAD_PATH=./uploads

# ================================
# RENDER-SPECIFIC NOTES:
# ================================
# 1. Set PORT to 10000 (Render's default)
# 2. Mark sensitive variables as "Secret" in Render dashboard
# 3. DATABASE_URL will be provided by Render PostgreSQL service
# 4. REDIS_URL can be from Render Redis or external service
# 5. All other variables are optional but recommended for full functionality
